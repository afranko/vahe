/* main.c - Only for tasks!
  Call your own task in the main loop and/or call
  an initialization function before the main loop if it's needed.
  */

/* Includes */
#include "freertos/FreeRTOS.h"
#include "freertos/task.h"
#include "freertos/queue.h"
#include "esp_system.h"

void app_main()
{
    //INIT

    for(;;)
    {
        //TASKS
    }
}
